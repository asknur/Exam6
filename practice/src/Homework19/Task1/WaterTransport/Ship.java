package Homework19.Task1.WaterTransport;

public class Ship extends WaterTransport {
    public Ship(String name, int model, int engineCapacity, int speed) {
        super(name, model, engineCapacity, speed);
    }
}
